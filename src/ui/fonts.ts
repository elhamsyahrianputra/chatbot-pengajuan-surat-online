import { Roboto, Roboto_Slab, Public_Sans } from "next/font/google";

export const roboto = Roboto({
    subsets: ["latin"],
});

export const robotoSlab = Roboto_Slab({
    subsets: ["latin"],
});

export const publicSans = Public_Sans({
    subsets: ["latin"],
});
